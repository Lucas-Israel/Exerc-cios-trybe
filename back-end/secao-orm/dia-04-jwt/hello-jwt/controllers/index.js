const ping = require('./ping');
const login = require('./login');
const me = require('./me');
const secret = require('./top-secret');
const signup = require('./signup');

module.exports = {
  ping,
  login,
  me,
  secret,
  signup,
};
